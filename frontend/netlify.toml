[build]
    base = "frontend"
    command = "npm run build"
    publish = ".next"
    environment = { SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_GOOGLE_CLIENT_ID,NEXT_PUBLIC_BACKEND_URI" }

[[plugins]]
    package = "@netlify/plugin-nextjs"